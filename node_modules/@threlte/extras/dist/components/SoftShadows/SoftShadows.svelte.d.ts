import { SvelteComponentTyped } from "svelte";
declare const __propDef: {
    props: {
        /** Size of the light source (the larger the softer the light), default: 25 */ size?: number | undefined;
        /** Depth focus, use it to shift the focal point (where the shadow is the sharpest), default: 0 (the beginning) */ focus?: number | undefined;
        /** Number of samples (more samples less noise but more expensive), default: 10 */ samples?: number | undefined;
    };
    events: {
        [evt: string]: CustomEvent<any>;
    };
    slots: {};
};
export type SoftShadowsProps = typeof __propDef.props;
export type SoftShadowsEvents = typeof __propDef.events;
export type SoftShadowsSlots = typeof __propDef.slots;
/**
 * This component injects Percentage-Closer Soft Shadows (PCSS) into
 * Three.js' shader chunk. Mounting and unmounting this component will lead to
 * all shaders being be re-compiled, although it will only cause overhead if
 * `<SoftShadows>` is mounted after the scene has already rendered, if it mounts
 * with everything else in your scene shaders will compile naturally.
 */
export default class SoftShadows extends SvelteComponentTyped<SoftShadowsProps, SoftShadowsEvents, SoftShadowsSlots> {
}
export {};
